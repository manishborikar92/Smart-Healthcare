{
  "version": 2,
  "builds": [
    { "src": "app.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/predict", "dest": "/api/predict.js" },
    { "src": "/(.*)", "dest": "/app.js" }
  ],
  "env": {
    "PORT": "3000"
  }
}
